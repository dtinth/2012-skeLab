<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= content_for?(:title) ? yield(:title) : "skeLab" %></title>
<%= csrf_meta_tags %>
<!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
<![endif]-->
<%= stylesheet_link_tag "application", :media => "all" %>
<%= javascript_include_tag "application" %>

</head>
<body>

<!--<%= request.ip %>-->
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="<%= root_path %>">SkeLab</a>
      <div class="container nav-collapse">
        <ul class="nav">
          <li><%= link_to raw("<i class=icon-home></i> Home"), root_path %></li>
          <% if current_user %>
            <li><%= link_to raw("<i class=icon-tasks></i> Tasks"), tasks_path %></li>
            <% if is_admin %>
              <li><%= link_to raw("<i class=icon-group></i> Attendance Checking"), attendances_path %></li>
              <li><%= link_to raw("<i class=icon-dashboard></i> Dashboard"), users_path %></li>
            <% end %>
            <li><%= link_to raw("<i class=icon-key></i> Logout (#{ current_user.name })"), logout_path %></li>
          <% else %>
            <li><%= link_to raw("<i class=icon-key></i> Login"), login_path %></li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

<% if current_user && is_checking_attendance && !is_current_user_attending %>
  <div class="container">
    <%= form_tag attendances_path do %>
      <div class="well attendance-checking-notification">
        <div>We are checking your attendance! Please mark your attendance:
          <button type="submit" name="mark" class="btn btn-success"><i class=icon-group></i> Mark My Attendance</button>
          </div>
      </div>
    <% end %>
  </div>
<% end %>
<% if current_user && is_admin && is_checking_attendance %>
  <div class="container">
    <%= form_tag attendances_path do %>
      <div class="well attendance-checking-notification">
        <div>Attendance checking is active!
          <button type="submit" name="stop" class="btn btn-inverse"><i class=icon-remove></i> Stop Checking</button>
          </div>
      </div>
    <% end %>
  </div>
<% end %>

<%= content_for?(:content) ? yield(:content) : yield %>

<footer>
  <p>&copy; made by ske09</p>
</footer>

</div> <!-- /container -->


</body>
</html>
